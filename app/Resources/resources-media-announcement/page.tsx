import React from "react";
import MediaAnnouncement from "../../../components/MediaAnnouncement";

interface Props {}

const index = (props: Props) => {
  return <MediaAnnouncement />;
};

export default index;
